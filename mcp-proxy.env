# MCP Proxy Server Configuration
# Edit this file to change ports and the config path.
# Do NOT edit the scripts directly.

# Port for the MCP proxy SSE endpoint
PROXY_PORT=8001

# Path to the MCP server definitions JSON
MCP_PROXY_CONFIG=/home/tipu/Development/mcp-proxy-server/mcp-proxy.json
